{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Crow App",
      "type": "cppdbg",
      "request": "launch",
      "program": "${workspaceFolder}/app.exe",
      "preLaunchTask": "build crow app",
      "cwd": "${workspaceFolder}",
      "MIMode": "gdb"
    }
  ]
}